source :rubygems

#TODO: Template: If your creating a gem add your gem directives here.
#      If not ignore this file and create the file /src/Gemfile and add them in that file

gem "rack"
gem "json_pure"
gem "<%= @config["project"]["name"]%>", "<%= @config["project"]["version"]%>", :path => "./"

group :test do
	gem "rspec"
	gem "rspec-core"
	gem "rspec-expectations"
	gem "rspec-mocks"
	gem "rcov"
end
